#include "includes.h"
#include "ds.h"

using namespace std;

// REQUIRES n is a 32 bit integer
// Returns true if n is a power of 2, false if it isnt.
bool isPowerOfTwo(int n);

long long addLinkedList(node * n1, node * n2);

